@model List<ResultProjectDto>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<link href="~/lib/bootstrap/dist/css/bootstrap.min.css" rel="stylesheet" />

<div class="container mt-5">
    <div class="card shadow-lg border-0 rounded-3">

        <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center">
            <h4 class="mb-0 fw-semibold">📋 Proje Listesi</h4>
            <a href="/Admin/Project/CreateProject" class="btn btn-light btn-sm fw-semibold">
                <i class="fa fa-plus-circle me-1"></i> Yeni Proje Ekle
            </a>
        </div>

        <div class="card-body p-4">
            @if (Model != null && Model.Any())
            {
                <table class="table table-striped table-bordered align-middle text-center">
                    <thead class="table-primary">
                        <tr>
                            <th scope="col">#</th>
                            <th scope="col">Image Url</th>
                            <th scope="col">Kategori</th>
                            <th scope="col" style="width: 200px;">İşlemler</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var project in Model)
                        {
                            <tr>
                                <td>@project.Id</td>
                                <td><img src="@project.ImageUrl" style="height:150px;width:150px" alt="Alternate Text" /></td>
                                <td>@project.Category.Name</td>
                                <td>
                                    <a href="/Admin/Category/UpdateCategory/@project.Id" class="btn btn-warning btn-sm me-2">
                                        <i class="fa fa-edit"></i> Güncelle
                                    </a>
                                    <a href="/Admin/Project/DeleteProject/@project.Id" class="btn btn-danger btn-sm"
                                       onclick="return confirm('Bu projeyi silmek istediğinize emin misiniz?');">
                                        <i class="fa fa-trash"></i> Sil
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            }
            else
            {
                <div class="alert alert-info text-center" role="alert">
                    📭 Henüz herhangi bir kategori eklenmemiş.
                </div>
            }
        </div>
    </div>
</div>